export interface UTR {
  id: string;
  uatId: string; // Reference to the City Hall (User ID)
  name: string; // e.g., "L1 - Locuințe individuale"
  technicalRegime: string; // Regimul tehnic și urbanistic
  restrictions: string; // Servituți și restricții
  createdAt: string;
}
